def countBattleships(board: list[list[str]]) -> int:
    battleships = 0
    board_rows = []
    board_cols = []
    for i in range(len(board)):
        board_rows.append(board[i])

    for i in range(len(board)):
        pass
    return battleships